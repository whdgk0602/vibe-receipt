import 'package:flutter/material.dart';

/// 앱 공통 컬러 팔레트
class AppColors {
  AppColors._();

  // 기본
  static const Color background = Color(0xFFF7F5F0); // 크림 화이트
  static const Color surface = Color(0xFFFFFFFF);
  static const Color primary = Color(0xFF2B2B2B); // 영수증 텍스트 (차콜)
  static const Color secondary = Color(0xFF8A8A8A);
  static const Color divider = Color(0xFFE5E5E5);

  // CTA
  static const Color accent = Color(0xFF2B2B2B);
  static const Color accentLight = Color(0xFFF0EDE5);
}
